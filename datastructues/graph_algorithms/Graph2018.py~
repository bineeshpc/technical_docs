if __name__ == '__main__':
    g = Graph()
    vertices = range(1, 6)
    for vertex in vertices:
        g.addvertex(vertex)

    edges = [(1, 2), (1,3), (1, 5), (2, 3), (2, 5)]
    for edge in edges:
        g.addedge(edge)

    
